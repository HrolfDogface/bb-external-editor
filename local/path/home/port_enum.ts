export enum PortNumber {
    phase = 5,
    hackPercentage = 10,
    travelFlag = 11,
    autoWorkFlag = 12,
    sleeveTask = 13,
    destroyBitnodeFlag = 14,
    joinFlag = 15,
    nextBitnode = 16,
}

export enum Phase {
    murderParty,
    buyRam,
    gangStartup1, //recruit bangers
    gangStartup2, //buy banger augments
    gangGang, //buy slum snakes augments
    stackNFG,
    daedalusRep, 
    stackNFG2, // destroy world demon if destruction is enabled, may need to stack more NFG to get to needed level
}